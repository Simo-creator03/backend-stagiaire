/*
package com.skysoft.easyschool.service;

import com.skysoft.easyschool.domain.Stage;
import com.skysoft.easyschool.dto.StageDTO;
import javassist.NotFoundException;

import java.util.List;

public interface StageService {

    Stage saveStage(StageDTO stageDTO) throws NotFoundException;
    Stage updateStage(StageDTO stageDTO) throws NotFoundException;
    void deleteStageById(Long id_stage);
    Stage getStage(Long id_stage);
    List<Stage> getAllStage();
}
*/
